name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Build without publishing a release"
        type: boolean
        default: true

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions: {}

env:
  CARGO_TERM_COLOR: always
  BINARY: nodex

jobs:
  verify_version:
    name: Verify version
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    outputs:
      version: ${{ steps.resolve.outputs.version }}
    steps:
      - uses: actions/checkout@v6

      - id: resolve
        name: Resolve and verify version
        shell: bash
        run: |
          cargo_version=$(grep -m1 '^version' Cargo.toml | cut -d'"' -f2)
          if [ "${GITHUB_REF_TYPE:-}" = "tag" ]; then
            tag_version="${GITHUB_REF_NAME#v}"
            if [ "$tag_version" != "$cargo_version" ]; then
              echo "::error::Tag v$tag_version does not match Cargo.toml version $cargo_version"
              exit 1
            fi
            version="$tag_version"
          else
            version="$cargo_version"
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "Resolved version: $version"

  build:
    name: ${{ matrix.label }}
    needs: verify_version
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      id-token: write
      attestations: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: linux-x64
            target: x86_64-unknown-linux-musl
            os: ubuntu-24.04
            archive: tar.gz
            builder: cross
          - label: linux-arm64
            target: aarch64-unknown-linux-musl
            os: ubuntu-24.04
            archive: tar.gz
            builder: cross
          - label: macos-universal
            target: universal-apple-darwin
            os: macos-15
            archive: tar.gz
            builder: lipo
          - label: windows-x64
            target: x86_64-pc-windows-msvc
            os: windows-2025
            archive: zip
            builder: cargo
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.builder == 'lipo' && 'aarch64-apple-darwin,x86_64-apple-darwin' || matrix.target }}

      - uses: Swatinem/rust-cache@v2.9.1
        with:
          key: ${{ matrix.target }}

      - name: Install cross
        if: matrix.builder == 'cross'
        uses: taiki-e/install-action@cross

      - name: Build (cross)
        if: matrix.builder == 'cross'
        run: cross build --release --locked --target ${{ matrix.target }} --package nodex-cli

      - name: Build (cargo)
        if: matrix.builder == 'cargo'
        run: cargo build --release --locked --target ${{ matrix.target }} --package nodex-cli

      - name: Build (lipo — macOS universal2)
        if: matrix.builder == 'lipo'
        shell: bash
        run: |
          cargo build --release --locked --target aarch64-apple-darwin --package nodex-cli
          cargo build --release --locked --target x86_64-apple-darwin --package nodex-cli
          mkdir -p target/${{ matrix.target }}/release
          lipo -create \
            target/aarch64-apple-darwin/release/${BINARY} \
            target/x86_64-apple-darwin/release/${BINARY} \
            -output target/${{ matrix.target }}/release/${BINARY}
          lipo -info target/${{ matrix.target }}/release/${BINARY}
          # Ad-hoc codesign so Gatekeeper accepts the binary without the
          # install script (manual-download users skip install.sh's signing).
          codesign --force --sign - target/${{ matrix.target }}/release/${BINARY}
          codesign --verify --verbose target/${{ matrix.target }}/release/${BINARY}

      - name: Package (unix)
        if: matrix.archive == 'tar.gz'
        shell: bash
        run: |
          version="${{ needs.verify_version.outputs.version }}"
          archive="${BINARY}-v${version}-${{ matrix.target }}.tar.gz"
          tar -C "target/${{ matrix.target }}/release" -czf "$archive" "$BINARY"
          shasum -a 256 "$archive" > "$archive.sha256"
          echo "ARCHIVE=$archive" >> "$GITHUB_ENV"

      - name: Package (windows)
        if: matrix.archive == 'zip'
        shell: pwsh
        run: |
          $version = "${{ needs.verify_version.outputs.version }}"
          $archive = "$env:BINARY-v$version-${{ matrix.target }}.zip"
          Compress-Archive -Path "target/${{ matrix.target }}/release/$env:BINARY.exe" -DestinationPath $archive
          $hash = (Get-FileHash $archive -Algorithm SHA256).Hash.ToLower()
          # Write the .sha256 as UTF-8 (no BOM) with exactly one trailing LF,
          # matching coreutils `sha256sum` format so shasum -c and aggregate
          # concatenation in the checksums job both succeed.
          [System.IO.File]::WriteAllText("$archive.sha256", "$hash  $archive`n")
          # GITHUB_ENV must be plain UTF-8; Out-File defaults on PS 5.1 add
          # a BOM that GHA fails to parse. Add-Content on pwsh 7+ writes UTF-8
          # without BOM by default.
          Add-Content -Path $env:GITHUB_ENV -Value "ARCHIVE=$archive"

      - name: Attest build provenance
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: ${{ env.ARCHIVE }}

      - uses: actions/upload-artifact@v7
        with:
          name: release-${{ matrix.label }}
          path: |
            ${{ env.ARCHIVE }}
            ${{ env.ARCHIVE }}.sha256
          retention-days: 7
          if-no-files-found: error

  # Package the Claude Code skill as a release asset so remote installers
  # (curl | bash) can fetch the entire skill directory atomically, instead
  # of trying to reconstruct it file-by-file from raw.githubusercontent.com.
  skill:
    name: Package skill
    needs: verify_version
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      id-token: write
      attestations: write
    steps:
      - uses: actions/checkout@v6
      - name: Package skill tarball
        shell: bash
        run: |
          version="${{ needs.verify_version.outputs.version }}"
          archive="${BINARY}-skill-v${version}.tar.gz"
          tar -C .claude/skills -czf "$archive" nodex
          shasum -a 256 "$archive" > "$archive.sha256"
          echo "ARCHIVE=$archive" >> "$GITHUB_ENV"
      - name: Attest build provenance
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: ${{ env.ARCHIVE }}

      - uses: actions/upload-artifact@v7
        with:
          name: release-skill
          path: |
            ${{ env.ARCHIVE }}
            ${{ env.ARCHIVE }}.sha256
          retention-days: 7
          if-no-files-found: error

  # Gate the release on the classified envelope-schema diff against the
  # previous release's published schema asset, and publish the two
  # config-independent manifests as pinnable contract artifacts. Only
  # these are published: schema/enums/rules depend on a project's
  # nodex.toml and would be misleading as release assets.
  contract:
    name: Contract gate
    needs: verify_version
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    env:
      VERSION: ${{ needs.verify_version.outputs.version }}
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2.9.1
        with:
          key: contract-gate

      - name: Build host binaries
        run: cargo build --release --locked --package nodex-cli

      - name: Emit contract manifests
        shell: bash
        run: |
          ./target/release/${BINARY} export envelope-schema > "nodex-envelope-schema-v${VERSION}.json"
          ./target/release/${BINARY} export commands > "nodex-commands-v${VERSION}.json"

      - name: Gate against the previous release's schema asset
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          # Fail-closed: this step runs under `bash -e`, so any gh
          # query or download failure (auth, network, API) fails the
          # job. Only a *successful* query showing nothing to diff
          # against — zero published releases, or a latest release
          # without a schema asset — is the genuine bootstrap, which
          # skips with a notice.
          latest=$(gh release list --repo "$GITHUB_REPOSITORY" --limit 1 --json tagName --jq '.[0].tagName')
          if [ -z "$latest" ] || [ "$latest" = "null" ]; then
            echo "::notice::no published release to diff against — the contract gate engages once a release ships a schema asset"
            echo '{"skipped":"no previous release"}' > contract-diff.json
            cat contract-diff.json >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi
          # Probe asset presence explicitly before downloading: a
          # successful view that lists no matching asset is bootstrap;
          # a failing view or download is an infra failure and fails
          # the job — never silently skipped.
          asset=$(gh release view "$latest" --repo "$GITHUB_REPOSITORY" --json assets --jq '[.assets[].name | select(startswith("nodex-envelope-schema-v") and endswith(".json"))] | first')
          if [ -z "$asset" ] || [ "$asset" = "null" ]; then
            echo "::notice::release $latest publishes no envelope-schema asset — the contract gate engages once a release ships one"
            echo "{\"skipped\":\"$latest has no schema asset\"}" > contract-diff.json
            cat contract-diff.json >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi
          gh release download "$latest" --repo "$GITHUB_REPOSITORY" --pattern 'nodex-envelope-schema-v*.json' --output baseline-envelope-schema.json
          set +e
          ./target/release/contract-gate baseline-envelope-schema.json "nodex-envelope-schema-v${VERSION}.json" > contract-diff.json
          status=$?
          set -e
          cat contract-diff.json >> "$GITHUB_STEP_SUMMARY"
          exit $status

      - uses: actions/upload-artifact@v7
        with:
          name: release-contract
          path: |
            nodex-envelope-schema-v*.json
            nodex-commands-v*.json
            contract-diff.json
          retention-days: 7
          if-no-files-found: error

  checksums:
    name: Aggregate checksums
    needs: [verify_version, build, skill]
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - uses: actions/download-artifact@v8
        with:
          path: artifacts
          pattern: release-*
          merge-multiple: true

      - name: Build SHA256SUMS.txt
        shell: bash
        run: |
          cd artifacts
          cat *.sha256 | sort -k2 > SHA256SUMS.txt
          echo "── SHA256SUMS.txt ──"
          cat SHA256SUMS.txt

      - uses: actions/upload-artifact@v7
        with:
          name: checksums
          path: artifacts/SHA256SUMS.txt
          retention-days: 7
          if-no-files-found: error

  release:
    name: Publish release
    needs: [verify_version, build, skill, checksums, contract]
    if: github.event_name == 'push' || github.event.inputs.dry_run == 'false'
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6

      - uses: actions/download-artifact@v8
        with:
          path: staged

      - name: Flatten release assets
        shell: bash
        run: |
          mkdir -p release
          find staged -type f \( -name '*.tar.gz' -o -name '*.zip' -o -name '*.sha256' -o -name 'SHA256SUMS.txt' -o -name '*.json' \) -exec cp {} release/ \;
          ls -lh release

      - uses: softprops/action-gh-release@v3
        with:
          tag_name: v${{ needs.verify_version.outputs.version }}
          files: release/*
          generate_release_notes: true
          fail_on_unmatched_files: true
